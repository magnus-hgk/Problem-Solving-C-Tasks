/*
    Programmer: Magnus Høgh             Date: 02-10-2025
    Instructor: Anders Schlichtkrull    Class: AAU Imperative Programming E25
    Write assignment expressions for the following statements
*/
#include <stdio.h>

int main(void)
{
    int n, k, between, divisor, digit, num, lowercase;
    char ch;

    between = -k < n && n < k;

    divisor = ((digit != 0) && num % digit == 0);

    lowercase = ('a' <= ch && ch <= 'z');
    
}